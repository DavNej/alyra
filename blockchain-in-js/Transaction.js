class Transaction {
  // from: address
  // to: address
  // amount: number
  // data: number

  constructor(from, to, amount, data) {
    this.from = from
    this.to = to
    this.amount = amount
    this.data = data
  }
}
